{
  "name": "analyticsservice-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["analyticsservice"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/analyticsservice-e2e/jest.config.ts",
        "passWithNoTests": true
      },
      "dependsOn": ["analyticsservice:build", "analyticsservice:serve"]
    }
  }
}
